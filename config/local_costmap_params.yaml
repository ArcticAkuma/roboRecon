local_costmap:
  global_frame: rtabmap/odom
  robot_base_frame: camera_link
  update_frequency: 5.0
  publish_frequency: 2.0
  static_map: false         #todo If you aren't using an existing map or map server, set the static_map parameter to false.
  rolling_window: true  #todo
  width: 6.0
  height: 6.0
  resolution: 0.05